{
  "name": "web-experiments",
  "version": "0.0.0",
  "dependencies": {},
  "devDependencies": {
    "browser-sync": "^2.0.0-rc8",
    "watch": "^0.13.0",
    "6to5": "^3.4.1"
  },
  "scripts": {
    "install": "npm install && bower install",
    "browser-sync": "browser-sync start --server --directory",
    "6to5": "6to5 experiments --out-dir experiments",
    "serve": "npm run browser-sync"
  }
}
